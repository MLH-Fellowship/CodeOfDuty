import React from "react";
import { withStyles } from "@material-ui/core";

const style = {};

class SprintLayout extends React.Component {
  constructor(props) {
    super(props);
    this.setState({});
  }

  render() {
    return <div>SprintLayout</div>;
  }
}

export default withStyles(style)(SprintLayout);
